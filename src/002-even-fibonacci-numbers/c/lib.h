#include <stdint.h>
#include <stdlib.h>
#include <stdio.h>
#include <math.h>
#pragma once

static const double PHI = 1.61803398875;
static const double PHI3 = PHI * PHI * PHI;

int solve(int max);
double next_even_fibonacci(double fibo);
