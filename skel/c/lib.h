#include <stdint.h>
#include <stdlib.h>
#include <stdio.h>
#include <math.h>
#pragma once

int solve(int max, int div1, int div2);
int sum_divisible(int max, int divisor);
int gcd(int a, int b);
int lcm(int a, int b);
